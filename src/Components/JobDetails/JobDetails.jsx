import React, { useEffect, useState } from 'react';
import { useLoaderData } from 'react-router-dom';

const JobDetails = () => {
    const id = useLoaderData()


console.log(Features)


    return (
        <div>
            <h1>Hello this is job details </h1>
            
        </div>
    );
};

export default JobDetails;